spring.application.name=fake-football
spring.jpa.hibernate.ddl-auto: update
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/fake_football}
# jdbc:postgresql://localhost:5432/fake_football
spring.datasource.username=${POSTGRES_USERNAME:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:rootroot}
